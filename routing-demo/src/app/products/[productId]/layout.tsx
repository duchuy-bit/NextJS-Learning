export default function ProductDetailLayout({children}: {children: React.ReactNode}) {
    return (
        <section>
            {children}
            <h2> Feature Layout</h2>
        </section>
    );
}